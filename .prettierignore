node_modules
dist
coverage
pnpm-lock.yaml

# Managed by changesets — changesets rewrites this file in its own format
# (2-space indent) on every version bump, so prettier shouldn't check it.
.changeset/pre.json

# Generated by changesets on `version` — markdown shape doesn't match our
# prettier config and round-trips as noise. Every release would otherwise
# fail format:check on the merge of the Version Packages PR.
**/CHANGELOG.md
