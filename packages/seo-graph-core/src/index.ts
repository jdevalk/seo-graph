// @jdevalk/seo-graph-core — agent-ready schema.org JSON-LD graph builders.
//
// Phase 1 will populate this barrel with:
//   - piece builders (article, webpage, website, breadcrumb, image,
//     person, organization, video, navigation, custom)
//   - makeIds factory
//   - assembleGraph + deduplicateByGraphId
//
// Types flow from schema-dts; this package is a thin linking + assembly layer.
export {};
